/// Refresh module, drawing LineSegs from BSP.
const std = @import("std");
const Defs = @import("definitions.zig");

pub fn renderMaskedSegmentRange(
    ds: *Defs.DrawSegment,
    x1: i16,
    x2: i16,
) void {
    _ = ds;
    _ = x1;
    _ = x2;
}
